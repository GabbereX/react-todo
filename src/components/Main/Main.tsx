import React, { FC } from 'react';
import styles from './Main.module.scss';

const Main: FC = () => {
  return (
    <main className={'main'}>
      <div className='container'>Main</div>
    </main>
  );
};

export default Main;
